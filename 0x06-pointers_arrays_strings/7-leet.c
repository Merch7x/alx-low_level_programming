/**
 * leet - encode string to a number
 * @s: array to be encoded
 * Return: returns a char
 */
char *leet(char *s)
{
	int i = 0;

	while ((s[i] => 'a' && s[i] <= 'z') && s[i] != '\0')
	{
		if (s[i] || s[i] += 32)
		{



